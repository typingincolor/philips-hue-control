export const STORAGE_KEYS = {
  BRIDGE_IP: 'hue_bridge_ip',
  USERNAME: 'hue_username', // Legacy, kept for migration
  SESSION_TOKEN: 'hue_session_token',
  SESSION_EXPIRES_AT: 'hue_session_expires_at',
  // Weather settings
  WEATHER_LOCATION: 'hue_weather_location', // { lat, lon, name }
  WEATHER_SETTINGS: 'hue_weather_settings', // { units, enabled }
};
